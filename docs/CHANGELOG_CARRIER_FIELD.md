# Изменение поля CarrierId → Carrier

## 📋 Краткое описание

Поле `carrierId` (GUID) заменено на `carrier` (string) для упрощения работы с транспортными компаниями.

## 🔄 Что изменилось

### Было:
```json
{
  "deliveryType": 2,
  "deliveryAddressId": "123e4567-e89b-12d3-a456-426614174003",
  "carrierId": "123e4567-e89b-12d3-a456-426614174005"
}
```

### Стало:
```json
{
  "deliveryType": 2,
  "deliveryAddressId": "123e4567-e89b-12d3-a456-426614174003",
  "carrier": "СДЭК"
}
```

## 📝 Изменения в API

### Эндпоинты, которые нужно обновить:

1. **POST /api/cart/create-order** - создание заказа из корзины
2. **POST /api/orders** - создание заказа напрямую

### Параметры запроса:

| Поле | Было | Стало | Описание |
|------|------|-------|----------|
| `carrierId` | `Guid?` | ❌ Удалено | ID транспортной компании |
| `carrier` | ❌ Не было | ✅ `string?` | Название транспортной компании |

### Ответ API:

В ответе заказа (`OrderDto`) теперь возвращается:
```json
{
  "id": "...",
  "orderNumber": "...",
  "status": 1,
  "deliveryType": 2,
  "carrier": "СДЭК",  // ← Новое поле
  "trackingNumber": "...",
  ...
}
```

## ✅ Что нужно сделать на фронтенде

### 1. Обновить модели/DTO

**C# (MVC):**
```csharp
// Было:
public Guid? CarrierId { get; set; }

// Стало:
public string? Carrier { get; set; }
```

**TypeScript (если используется):**
```typescript
// Было:
carrierId?: string;

// Стало:
carrier?: string;
```

### 2. Обновить формы создания заказа

**Пример для Razor:**
```html
<!-- Было: -->
<input type="hidden" name="CarrierId" value="@Model.CarrierId" />

<!-- Стало: -->
<input type="text" name="Carrier" value="@Model.Carrier" placeholder="Название транспортной компании" />
```

**Пример для JavaScript:**
```javascript
// Было:
const orderData = {
    deliveryType: 2,
    deliveryAddressId: addressId,
    carrierId: selectedCarrierId  // ❌
};

// Стало:
const orderData = {
    deliveryType: 2,
    deliveryAddressId: addressId,
    carrier: selectedCarrierName  // ✅ Например: "СДЭК", "Почта России", "Boxberry"
};
```

### 3. Обновить валидацию

**C# (Model/ViewModel):**
```csharp
// Было:
[Display(Name = "Транспортная компания")]
public Guid? CarrierId { get; set; }

// Стало:
[Display(Name = "Транспортная компания")]
[StringLength(500)]
public string? Carrier { get; set; }
```

### 4. Обновить логику выбора транспортной компании

Если раньше был список с GUID:
```csharp
// Было:
var carriers = new List<SelectListItem>
{
    new SelectListItem { Value = "guid-1", Text = "СДЭК" },
    new SelectListItem { Value = "guid-2", Text = "Почта России" }
};

// Стало (просто список названий):
var carriers = new List<string> { "СДЭК", "Почта России", "Boxberry" };
```

Или можно использовать выпадающий список:
```html
<select name="Carrier" class="form-control">
    <option value="">Выберите транспортную компанию</option>
    <option value="СДЭК">СДЭК</option>
    <option value="Почта России">Почта России</option>
    <option value="Boxberry">Boxberry</option>
    <option value="Яндекс.Доставка">Яндекс.Доставка</option>
</select>
```

### 5. Обновить отображение заказа

**Razor:**
```html
<!-- Было: -->
@if (order.CarrierId.HasValue)
{
    <p>Транспортная компания: @order.CarrierId</p>
}

<!-- Стало: -->
@if (!string.IsNullOrEmpty(order.Carrier))
{
    <p>Транспортная компания: @order.Carrier</p>
}
```

## ⚠️ Важные моменты

1. **Тип доставки Carrier (2)** - поле `carrier` **обязательно** должно быть заполнено
2. **Для других типов доставки** - поле `carrier` можно не передавать (или передать `null`)
3. **Валидация** - теперь не нужно проверять формат GUID, достаточно проверить, что строка не пустая
4. **Максимальная длина** - название транспортной компании ограничено 500 символами

## 🔍 Примеры использования

### Создание заказа с доставкой транспортной компанией:

```csharp
var createOrderDto = new CreateOrderFromCartDto
{
    DeliveryType = DeliveryType.Carrier,  // 2
    DeliveryAddressId = selectedAddressId,
    Carrier = "СДЭК",  // ← Просто строка
    CargoReceiverId = null
};
```

### Валидация на фронтенде:

```csharp
if (model.DeliveryType == DeliveryType.Carrier)
{
    if (string.IsNullOrWhiteSpace(model.Carrier))
    {
        ModelState.AddModelError("Carrier", "Укажите транспортную компанию");
    }
}
```

## 📅 Дата изменения

**2024-12-08** - Изменение типа поля `CarrierId` → `Carrier`

## ❓ Вопросы?

Если возникнут вопросы по миграции, обращайтесь к бэкенд-разработчику.

